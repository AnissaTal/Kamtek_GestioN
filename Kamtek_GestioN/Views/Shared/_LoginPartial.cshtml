@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager



    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

   

 

            @if (SignInManager.IsSignedIn(User))
            {

               
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light mb-3">
            <div class="container">
                <div class="dropdown">

                    <button class="btn  dropdown-toggle btn-info" type="button" id="btnGroupDrop1" data-toggle="dropdown" aria-haspopup="true">
                        Article
                    </button>

                    <div class="dropdown-menu" aria-labelledby="btnGroupDrop1">
                        <a class="dropdown-item" asp-controller="Articles" asp-action="Create">Ajouter Article</a>
                        <a class="dropdown-item" asp-controller="Articles" asp-action="Index">Lister Article</a>

                    </div>
                </div>
                <div class="dropdown">

                    <button class="btn btn-info dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Catégorie
                    </button>

                    <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">

                        <a class="dropdown-item" asp-controller="Categories" asp-action="Create">Ajouter Categorie</a>
                        <a class="dropdown-item" asp-controller="Categories" asp-action="Index">Lister Categorie</a>

                    </div>
                </div>

                <div class="dropdown">

                    <button class="btn btn-info dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Commande
                    </button>

                    <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">

                        <a class="dropdown-item" asp-controller="Commandes" asp-action="Create">Ajouter Commande</a>
                        <a class="dropdown-item" asp-controller="Commandes" asp-action="Index">Lister Commande</a>

                    </div>
                </div>
            </div>
        </nav>

            //rutpure bootstrap -----------------------------

    <div class="collapse navbar-collapse float-right" id="navbarSupportedContent3">
        <div>
            <div class="nav-item">
                <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity.Name!</a>
            </div>
            <div class="nav-item">
                    <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                        <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
                    </form>
            </div>
        </div>
   </div>


        }
        else
        {
        <div>
            <div class="nav-item">
                <a class="nav-link text-dark font-weight-bold" asp-area="Identity" asp-page="/Account/Register">S'inscrire</a>
            </div>
            <div class="nav-item">
                <a class="nav-link text-dark font-weight-bold" asp-area="Identity" asp-page="/Account/Login">Se connecter</a>
            </div>

        </div>
        }

    

